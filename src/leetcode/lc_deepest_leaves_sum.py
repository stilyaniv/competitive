from typing import List

# Definition for a binary tree node.
class TreeNode:
    def __init__(self, val=0, left=None, right=None):
        self.val = val
        self.left = left
        self.right = right


class Solution:
    def _arrayToTree(self, list: List[int]) -> TreeNode:
        pass

        # return TreeNode(val[0], left=self._arrayToTree(list:[1:]))

    def deepestLeavesSum(self, root: TreeNode) -> int:
        pass
